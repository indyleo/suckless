#!/bin/sh
# Same $PATH listing as dmenu_run, but in run mode (-r): Enter always
# executes exactly what's typed, so you can pick a suggestion and then
# add arguments before running it.
dmenu_path | dmenu -r -p "Run:" "$@" | ${SHELL:-"/bin/sh"} &
